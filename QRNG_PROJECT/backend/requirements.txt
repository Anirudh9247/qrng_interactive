# pinned dependencies generated via `pip freeze` in the backend virtualenv
# Note: use absolute equality (==) to ensure reproducible builds. To update,
# activate the venv and run something like:
#   pip install -U <package>
#   pip freeze | grep -E 'fastapi|uvicorn|SQLAlchemy|python-dotenv' > requirements.txt
# or maintain a constraints file and update it accordingly.

fastapi==0.135.1
uvicorn==0.41.0
SQLAlchemy==2.0.48
python-dotenv==1.2.2
qiskit
qiskit-aer
